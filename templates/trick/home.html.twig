{% extends "base.html.twig" %}
{% block body %}

    <header class="background">
      {#  <img src="images\background_image.jpg">#}
       <div class="container">

           <div class="intro-text" >
               <h2 class="intro-heading text-uppercase h1 py-5">SnowTricks</h2>
               <h1 class="intro-paragraph">Le site de la communauté de snowboard freestyle!!!</h1>

           </div>

       </div>
    </header>

    <div class="container my-5 py-5">
            <div class="card-block  col-md-12 d-md-flex ">
            {% for key,trick in tricks %}
                {% if key % 5 == 0 and key != 0%}
                </div>
                <div class="card-block col-md-12 d-md-flex mt-5">
                {% endif %}

                    <div class="card-body  col-md d-flex flex-column mt-4 mr-2">
                        <div class="card ">

                            <div class="mb-3 card-img-top d-flex justify-content-center">
                                <a href="/trick/{{ trick.id }}"><img  src="/images/background_image.jpg" height="100" width="100" alt="test"></a>
                            </div>
                        </div>

                    <div class="card">
                        <div class="card-body d-flex justify-content-around">
                            <a href="/trick/{{ trick.id }}">{{ trick.title }}</a>
                            <a href="/trick/{{ trick.id }}/edit"><img src="/images/edit-24px.svg" alt='Editer'> </a>

                         {#   <form method="post" target="#confirmDelete"#}{#action="/trick/{{ trick.id }}/delete" #}{#>#}
                                {#<input type="hidden" name="_method" value="DELETE">
                                 <input type="hidden" name="_token" value="{{ csrf_token('delete'~ trick.id) }}">#}

                            {% include "/template/modal_confirm_close.html.twig" %}
                            <a  data-toggle="modal" data-target="#confirmDelete{{ trick.id }}" ><img src="/images/delete-24px.svg" alt="Supprimer"></a>
                            {#</form>#}

                        </div>

                    </div>
                    </div>

                    {% endfor %}
                </div>


    </div>




















{% endblock %}