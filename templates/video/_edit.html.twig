

<div id="" class=" container d-flex">
    <div class="col-md-8 text-center">
        <div >
            <iframe frameborder="0" width="300" height="200" src="{{ video.name }}" allowfullscreen
                    type="text/html" allow="autoplay encrypted-media; gyroscope; picture-in-picture"></iframe>
        </div>
    </div>
    <div class="col-md-4" >
        {{ form_start(form, {'attr': {'id':'videoId', 'action':  path('video_edit', {'id':video.id}) }})  }}
        <div class="my-5">
            <div >
                {{ form_row(form.name, {'value': video.name}) }}
            </div>
            <div class="hidden">
                {{ form_rest(form) }}
            </div>
            </div>
        <div class="text-center mt-4">

            <div >
                <button type="submit" class="btn btn-primary">Sauvegarder</button>
            </div>

        </div>
        {{ form_end(form) }}
    </div>


</div>
